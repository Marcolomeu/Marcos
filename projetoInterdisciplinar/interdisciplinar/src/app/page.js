import React from 'react';
import Header from '../components/header/header.js';
import styles from '../app/page.module.css';
import { motion } from 'framer-motion';

export default function Page() {
  /*
    const text = 'Clínica Washington Coração Valente'
    return (
      <div className="App">
        {text.map((el, i) => (
          <motion.span
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{
            duration: 0.25,
            delay: i / 10
            }}
            key={i}
          >
            {el}{" "}
          </motion.span>
        ))}
      </div>
    );
    */
}



